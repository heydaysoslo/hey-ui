class Theme {
  constructor(theme) {
    this.theme = theme;
  }

  getTheme() {
    return this.theme;
  }

  setTheme(theme) {
    this.theme = theme;
  }

}

var init = new Theme({});

export default init;
//# sourceMappingURL=init.ts.modern.js.map
